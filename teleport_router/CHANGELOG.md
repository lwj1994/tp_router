# 0.8.3

* Complete removal of `CupertinoPage` and `MaterialPage` to fix the gray background issue during transitions.
* Default transition now uses `CustomTransitionPage` with `TeleportCupertinoPageTransition`.
